pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
rootProject.name = "TodoApp"
include ':app'
setBinding(new Binding([gradle: this]))
evaluate(new File(settingsDir, "flutter_todo/.android/include_flutter.groovy"))
